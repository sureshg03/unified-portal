"""
Django settings for backend project.

Generated by 'django-admin startproject' using Django 4.2.16.

For more information on this file, see
https://docs.djangoproject.com/en/4.2/topics/settings/

For the full list of settings and their values, see
https://docs.djangoproject.com/en/4.2/ref/settings/
"""

from pathlib import Path

# Build paths inside the project like this: BASE_DIR / 'subdir'.
BASE_DIR = Path(__file__).resolve().parent.parent


# Quick-start development settings - unsuitable for production
# See https://docs.djangoproject.com/en/4.2/howto/deployment/checklist/

# SECURITY WARNING: keep the secret key used in production secret!
SECRET_KEY = 'django-insecure-zq1qj^vfyywm&wa+c-4zks4_7pe(6-4%u49+410@^&xgyj!_j2'

# SECURITY WARNING: don't run with debug turned on in production!
DEBUG = True

ALLOWED_HOSTS = ['testserver', 'localhost', '127.0.0.1']


# Application definition

INSTALLED_APPS = [
    'django.contrib.admin',
    'django.contrib.auth',
    'django.contrib.contenttypes',
    'django.contrib.sessions',
    'django.contrib.messages',
    'django.contrib.staticfiles',
    'corsheaders',
    'rest_framework',
    'rest_framework.authtoken',
    # CDOE LSC Portal Apps
    'lsc_auth',
    'portal',
    'admissions',
    # Student Admission Portal Apps
    'api',
]

MIDDLEWARE = [
    'corsheaders.middleware.CorsMiddleware',
    'django.middleware.security.SecurityMiddleware',
    'django.contrib.sessions.middleware.SessionMiddleware',
    'django.middleware.common.CommonMiddleware',
    'django.middleware.csrf.CsrfViewMiddleware',
    'django.contrib.auth.middleware.AuthenticationMiddleware',
    'django.contrib.messages.middleware.MessageMiddleware',
    'django.middleware.clickjacking.XFrameOptionsMiddleware',
]

ROOT_URLCONF = 'backend.urls'

TEMPLATES = [
    {
        'BACKEND': 'django.template.backends.django.DjangoTemplates',
        'DIRS': [],
        'APP_DIRS': True,
        'OPTIONS': {
            'context_processors': [
                'django.template.context_processors.debug',
                'django.template.context_processors.request',
                'django.contrib.auth.context_processors.auth',
                'django.contrib.messages.context_processors.messages',
            ],
        },
    },
]

WSGI_APPLICATION = 'backend.wsgi.application'


# Database
# https://docs.djangoproject.com/en/4.2/ref/settings/#databases

# SQLite (commented out)
# DATABASES = {
#     'default': {
#         'ENGINE': 'django.db.backends.sqlite3',
#         'NAME': BASE_DIR / 'db.sqlite3',
#     }
# }

# MySQL Database Configuration - Triple Database Setup
DATABASES = {
    # Default database for Django admin, sessions, and LSC users
    'default': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'lsc_portal_db',  # LSC Portal database for LSC users
        'USER': 'root',
        'PASSWORD': '',  # Your MySQL password (empty = no password)
        'HOST': 'localhost',
        'PORT': '3306',
        'OPTIONS': {
            'charset': 'utf8mb4',
            'collation': 'utf8mb4_unicode_ci',
        },
    },
    # Secondary database for admin authentication
    'online_edu': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'online_edu',  # Online education database for admins
        'USER': 'root',
        'PASSWORD': '',  # Your MySQL password (empty = no password)
        'HOST': 'localhost',
        'PORT': '3306',
        'OPTIONS': {
            'charset': 'utf8mb4',
            'collation': 'utf8mb4_unicode_ci',
        },
    },
    # Admin database for portal app (ApplicationSettings, etc)
    'lsc_admindb': {
        'ENGINE': 'django.db.backends.mysql',
        'NAME': 'lsc_admindb',  # Admin database for portal models
        'USER': 'root',
        'PASSWORD': '',  # Your MySQL password (empty = no password)
        'HOST': 'localhost',
        'PORT': '3306',
        'OPTIONS': {
            'charset': 'utf8mb4',
            'collation': 'utf8mb4_unicode_ci',
        },
    }
}

# Database Router - Routes LSCAdmin to online_edu and LSCUser to default
DATABASE_ROUTERS = ['backend.db_router.LSCDatabaseRouter']

# Temporarily using SQLite to dump data
# DATABASES = {
#     'default': {
#         'ENGINE': 'django.db.backends.sqlite3',
#         'NAME': BASE_DIR / 'db.sqlite3',
#     }
# }


# Password validation
# https://docs.djangoproject.com/en/4.2/ref/settings/#auth-password-validators

AUTH_PASSWORD_VALIDATORS = [
    {
        'NAME': 'django.contrib.auth.password_validation.UserAttributeSimilarityValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.MinimumLengthValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.CommonPasswordValidator',
    },
    {
        'NAME': 'django.contrib.auth.password_validation.NumericPasswordValidator',
    },
]


# Internationalization
# https://docs.djangoproject.com/en/4.2/topics/i18n/

LANGUAGE_CODE = 'en-us'

TIME_ZONE = 'UTC'

USE_I18N = True

USE_TZ = True


# Static files (CSS, JavaScript, Images)
# https://docs.djangoproject.com/en/4.2/howto/static-files/

STATIC_URL = 'static/'

# Default primary key field type
# https://docs.djangoproject.com/en/4.2/ref/settings/#default-auto-field

DEFAULT_AUTO_FIELD = 'django.db.models.BigAutoField'

# Custom authentication backends - Dual Database Authentication
# Checks both online_edu.lsc_admins AND lsc_portal_db.lsc_auth_lscuser
AUTHENTICATION_BACKENDS = [
    'lsc_auth.auth_backend.DualDatabaseAuthBackend',  # Master: Checks both databases
    'django.contrib.auth.backends.ModelBackend',  # Fallback: Default Django auth
]

REST_FRAMEWORK = {
    'DEFAULT_AUTHENTICATION_CLASSES': [
        'lsc_auth.authentication.LSCJWTAuthentication',
        'rest_framework.authentication.TokenAuthentication',
        'rest_framework.authentication.SessionAuthentication',
        'rest_framework.authentication.BasicAuthentication',
    ],
    'DEFAULT_PERMISSION_CLASSES': [
        'rest_framework.permissions.IsAuthenticated',
    ],
}

# JWT Settings for Secure Authentication
from datetime import timedelta

SIMPLE_JWT = {
    'ACCESS_TOKEN_LIFETIME': timedelta(hours=2),  # Access token expires in 2 hours
    'REFRESH_TOKEN_LIFETIME': timedelta(days=7),  # Refresh token expires in 7 days
    'ROTATE_REFRESH_TOKENS': True,  # Generate new refresh token on refresh
    'BLACKLIST_AFTER_ROTATION': True,  # Blacklist old refresh tokens
    'UPDATE_LAST_LOGIN': True,  # Update last_login field on token generation
    
    'ALGORITHM': 'HS256',
    'SIGNING_KEY': SECRET_KEY,
    'VERIFYING_KEY': None,
    'AUDIENCE': None,
    'ISSUER': 'lsc-portal',
    
    'AUTH_HEADER_TYPES': ('Bearer',),
    'AUTH_HEADER_NAME': 'HTTP_AUTHORIZATION',
    'USER_ID_FIELD': 'id',
    'USER_ID_CLAIM': 'user_id',
    
    'AUTH_TOKEN_CLASSES': ('rest_framework_simplejwt.tokens.AccessToken',),
    'TOKEN_TYPE_CLAIM': 'token_type',
    
    'JTI_CLAIM': 'jti',
    
    # Custom claims
    'TOKEN_USER_CLASS': 'lsc_auth.models.LSCUser',
}

CORS_ALLOWED_ORIGINS = [
    "http://localhost:5173",  # Vite dev server
    "http://127.0.0.1:5173",
    "http://localhost:5175",  # Current Vite port
    "http://127.0.0.1:5175",
    "http://localhost:8080",  # Alternative Vite port
    "http://127.0.0.1:8080",
    "http://localhost:8082",  # Current Vite port
    "http://127.0.0.1:8082",
    # Add production URL when deployed
]

CORS_ALLOW_CREDENTIALS = True
CORS_ALLOW_METHODS = [
    'DELETE',
    'GET',
    'OPTIONS',
    'PATCH',
    'POST',
    'PUT',
]
CORS_ALLOW_HEADERS = [
    'accept',
    'accept-encoding',
    'authorization',
    'content-type',
    'dnt',
    'origin',
    'user-agent',
    'x-csrftoken',
    'x-requested-with',
]

# Media files configuration (for Student Admission Portal)
import os
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')

# Email Configuration (Student Admission Portal)
EMAIL_BACKEND = 'django.core.mail.backends.smtp.EmailBackend'
EMAIL_HOST = 'smtp.gmail.com'
EMAIL_PORT = 587
EMAIL_USE_TLS = True
EMAIL_HOST_USER = 'periyaruniversity08@gmail.com'
EMAIL_HOST_PASSWORD = 'wafl azrs ldjv eeyt'
DEFAULT_FROM_EMAIL = EMAIL_HOST_USER

# Payment Gateway Configuration (Student Admission Portal)
try:
    from decouple import config
    
    # Razorpay Configuration
    RAZORPAY_KEY_ID = config('RAZORPAY_KEY_ID', default='rzp_test_phCnHzNiGNxEc9')
    RAZORPAY_KEY_SECRET = config('RAZORPAY_KEY_SECRET', default='i837TRYWlZrLq4JTSTpGNx0q')
    
    # Paytm Configuration
    PAYTM_ENVIRONMENT = config('PAYTM_ENVIRONMENT', default='PROD')
    PAYTM_MERCHANT_KEY = config('PAYTM_MERCHANT_KEY', default="")
    PAYTM_MERCHANT_MID = config('PAYTM_MERCHANT_MID', default="")
    
    if PAYTM_ENVIRONMENT == 'PROD':
        PAYTM_MERCHANT_WEBSITE = config('PAYTM_MERCHANT_WEBSITE', default='DEFAULT')
        PAYTM_TXN_URL = config('PAYTM_TXN_URL', default='https://securegw.paytm.in/order/process')
        PAYTM_STATUS_QUERY_URL = config('PAYTM_STATUS_QUERY_URL', default='https://securegw.paytm.in/v3/order/status')
        PAYTM_REFUND_URL = config('PAYTM_REFUND_URL', default='https://securegw.paytm.in/refund/HANDLER_INTERNAL/REFUND')
    else:
        PAYTM_MERCHANT_WEBSITE = config('PAYTM_MERCHANT_WEBSITE', default='WEBSTAGING')
        PAYTM_TXN_URL = config('PAYTM_TXN_URL', default='https://securegw-stage.paytm.in/order/process')
        PAYTM_STATUS_QUERY_URL = config('PAYTM_STATUS_QUERY_URL', default='https://securegw-stage.paytm.in/v3/order/status')
        PAYTM_REFUND_URL = config('PAYTM_REFUND_URL', default='https://securegw-stage.paytm.in/refund/HANDLER_INTERNAL/REFUND')
    
    PAYTM_CALLBACK_URL = config('PAYTM_CALLBACK_URL', default='http://localhost:8000/api/pgResponse/')
except ImportError:
    # Fallback if decouple is not installed
    RAZORPAY_KEY_ID = 'rzp_test_phCnHzNiGNxEc9'
    RAZORPAY_KEY_SECRET = 'i837TRYWlZrLq4JTSTpGNx0q'
    PAYTM_ENVIRONMENT = 'PROD'
    PAYTM_MERCHANT_KEY = ""
    PAYTM_MERCHANT_MID = ""
    PAYTM_MERCHANT_WEBSITE = 'DEFAULT'
    PAYTM_TXN_URL = 'https://securegw.paytm.in/order/process'
    PAYTM_STATUS_QUERY_URL = 'https://securegw.paytm.in/v3/order/status'
    PAYTM_REFUND_URL = 'https://securegw.paytm.in/refund/HANDLER_INTERNAL/REFUND'
    PAYTM_CALLBACK_URL = 'http://localhost:8000/api/pgResponse/'

# Google Drive Configuration (Student Admission Portal)
GOOGLE_DRIVE_CREDENTIALS_FILE = 'credentials/application-form-461801-5f596cf183b9.json'
